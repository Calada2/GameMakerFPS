
var window_height = room_height;


var w = floor(room_width / 3)
var h = floor(room_height)


drawStart()
trace()
/*

for(var i = 0; i < w; ++i) if(i % 1 == 0)
{
	
	drawLine(i, (i/ 3), Sprite3, i)
}

for(var i = 0; i < w; ++i)  if(i % 1 == 0)
{
	drawLine(i + w, (w/ 3), Sprite2, i)
}

for(var i = 0; i < w; ++i)  if(i % 1 == 0)
{
	drawLine(i + w + w, ((w-i)/ 3), Sprite3, i)
}
*/

drawFinish()
drawFps()
